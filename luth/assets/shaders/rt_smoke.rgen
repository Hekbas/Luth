#version 460
#extension GL_EXT_ray_tracing : enable

// Validation-only smoke test (B.1.F) — proves RT pipeline + SBT + traceRays plumb together.
// Has no resources / no descriptors / no actual tracing. Destroyed at the end of Init.
// B.3 ships the first production RT raygen for shadows.
void main() {}
